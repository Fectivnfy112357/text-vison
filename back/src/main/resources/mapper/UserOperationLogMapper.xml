<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.textvision.mapper.UserOperationLogMapper">

    <!-- 分页查询用户操作日志 -->
    <select id="selectUserOperationLogsPage" resultType="com.textvision.entity.UserOperationLog">
        SELECT * FROM user_operation_logs 
        WHERE user_id = #{userId}
        <if test="operationType != null and operationType != ''">
            AND operation = #{operationType}
        </if>
        <if test="resourceType != null and resourceType != ''">
            AND resource_type = #{resourceType}
        </if>
        <if test="startTime != null">
            AND created_at >= #{startTime}
        </if>
        <if test="endTime != null">
            AND created_at <= #{endTime}
        </if>
        ORDER BY created_at DESC
    </select>

    <!-- 统计用户操作次数（带条件） -->
    <select id="countByUserIdAndConditions" resultType="long">
        SELECT COUNT(*) FROM user_operation_logs 
        WHERE user_id = #{userId}
        <if test="operationType != null and operationType != ''">
            AND operation = #{operationType}
        </if>
        <if test="startTime != null">
            AND created_at >= #{startTime}
        </if>
        <if test="endTime != null">
            AND created_at <= #{endTime}
        </if>
    </select>

    <!-- 查询用户最近的操作日志 -->
    <select id="selectRecentByUserId" resultType="com.textvision.entity.UserOperationLog">
        SELECT * FROM user_operation_logs 
        WHERE user_id = #{userId} 
        ORDER BY created_at DESC 
        LIMIT #{limit}
    </select>

    <!-- 删除过期日志 -->
    <delete id="deleteExpiredLogs">
        DELETE FROM user_operation_logs 
        WHERE created_at < #{beforeTime}
    </delete>

</mapper>